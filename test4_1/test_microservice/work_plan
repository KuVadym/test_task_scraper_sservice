План роботи на 3 дні (команда 4 людини)


День 1: Планування та базова структура
Ціль: Розбити задачу між учасниками, налаштувати середовище, визначити технології та структуру проєкту.

Розподіл ролей у команді:

Парсер: 1 людина

Проміжний сервіс: 1 людина

Публікація: 1 людина

DevOps/Оркестрація: 1 людина

Вибір стеку:

Парсер: Python + BeautifulSoup/Scrapy + requests

База даних: PostgreSQL (або SQLite для тестів)

Проміжний сервіс: FastAPI (або Flask)

Публікація: Python + requests

Оркестрація: Docker + Docker Compose

Фонові завдання: Celery + Redis

Налаштування середовища розробки:

Ініціалізація репозиторію GitHub/GitLab.

Створення базового Dockerfile та docker-compose.yml.

Розробка мінімального каркасу FastAPI для проміжного сервісу.

Перевірка підключення до БД.



День 2: Реалізація основних сервісів
Ціль: Написати основну логіку сервісів та перевірити взаємодію між ними.

Парсер (відповідальний 1):

Написати скрипт для отримання списку товарів за категорією.

Збереження результатів у проміжний сервіс через API.

Проміжний сервіс (відповідальний 2):

Реалізувати API для прийому товарів (POST /add_product).

Реалізувати API для отримання товарів (GET /products).

Реалізувати взаємодію з базою даних.

Скрипт публікації (відповідальний 3):

Отримання даних із проміжного сервісу.

Формування та відправка запитів на Poshmark.

Логування запитів і відповідей.

DevOps (відповідальний 4):

Налаштування контейнеризації всіх сервісів.

Створення docker-compose.override.yml для локального тестування.

Налаштування Celery для періодичного запуску парсера.



День 3: Тестування, налагодження та фіналізація
Ціль: Перевірити роботу мікросервісів разом, оптимізувати код і підготувати проєкт до здачі.

Тестування інтеграції між сервісами:

Написання тестів для API-проміжного сервісу.

Тестування парсера: правильність отримання даних.

Тестування публікації: чи коректно товари з'являються на Poshmark.

Оптимізація та виправлення помилок:

Виправлення помилок за результатами тестів.

Додавання таймаутів та обробки винятків.

Фіналізація проєкту:

Документація (README.md з інструкціями запуску).

Підготовка демонстрації роботи.

Підготовка до здачі проєкту.

Такий підхід дозволить ефективно розподілити завдання, протестувати систему та підготувати її до стабільної роботи. 🚀
